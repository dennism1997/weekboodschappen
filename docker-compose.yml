services:
  weekboodschappen:
    container_name: weekboodschappen
    build: .
    restart: unless-stopped
    ports:
      - "6883:6883"
    volumes:
      - app-data:/data
    environment:
      - DATABASE_PATH=/data/weekboodschappen.db
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - PORT=6883
      - APP_URL=${APP_URL}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - TRUSTED_ORIGINS=${TRUSTED_ORIGINS}
      - PASSKEY_ORIGIN=${PASSKEY_ORIGIN}
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:6883/api/health').then(r => { if (!r.ok) process.exit(1) }).catch(() => process.exit(1))"]
      interval: 3s
      timeout: 2s
      retries: 5

volumes:
  app-data:
